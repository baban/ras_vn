# Use the README file as a reference to customize settings.

RecipeFood:
  fields:
    default: recipe_food_genre_id, name, show_top
    form: recipe_food_genre_id, name, show_top
  relationships: recipes
  application: Application
