# Use the README file as a reference to customize settings.

RecipeFoodstuff:
  fields:
    default: recipe_id, name, amount
    form: recipe_id, name, amount
  relationships: 
  application: Application
