# Use the README file as a reference to customize settings.

SearchLog:
  fields:
    default: user_id, words, location
    form: user_id, words, location
  relationships: 
  application: Application
