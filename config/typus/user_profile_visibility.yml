# Use the README file as a reference to customize settings.

UserProfileVisibility:
  fields:
    default: user_profile_id, nickname, sex, first_name, family_name, birthday, email, prefecture_id
    form: user_profile_id, nickname, sex, first_name, family_name, birthday, email, prefecture_id
  relationships: 
  application: Application
