<div id="header">
  <div class="logo">
    ロゴ
  </div>
  <%= form_for :search, url:{ action:"index" }, html:{ class:"header_search" } do |f| %>
    <%= text_field_tag :word, "" %>
    <%= f.submit "検索" %>
  <% end %>
</div>
