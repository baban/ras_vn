<h1>Profile</h1>

<%=form_for :user_info, url:{ action:'update' } do |f| %>
  <table>
    <tr>
      <td>ニックネーム</td>
      <td colspan="2"><%= @info.nickname %></td>
    </tr>
    <tr>
      <td>メールアドレス</td>
      <td colspan="2"><%= @info.mail_address %></td>
    </tr>
    <tr>
      <td></td>
      <td>性</td>
      <td>名</td>
    </tr>
    <tr>
      <td></td>
      <td><%= @info.first_name %></td>
      <td><%= @info.last_name  %></td>
    </tr>
    <tr>
      <td>性別</td>
      <td colspan="2"><%=translate_sex( @info.sex ) %></td>
    </tr>
    <tr>
      <td>生年月日</td>
      <td colspan="2"><%= @info.birthday %></td>
    </tr>
    <tr>
      <td colspan="3"><%=link_to "編集", action:'edit', id: @info.id %></td>
    </tr>
  </table>
<% end %>
